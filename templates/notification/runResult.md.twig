The last runs ended with the result level "{{ worst_level }}".

Here is the detail:

{% for run in runs | filter(run => run.siteResult == worst_level) %}

## {{ run.site.name|e }}

{% for run_check_result in run.checkResults | filter(run_check_result => run_check_result.level == worst_level) %}
    * {{ ('check_result.type.' ~ run_check_result.type)|trans(run_check_result.data)|raw }}
{% endfor %}

{% endfor %}
